# Archived Source Material

This directory preserves the original brief that seeded NornRune's baseline
conformance policy, its text transcription, and nothing else. The files are
retained for provenance only: they are historical records and not current product requirements, and no active documentation, code, or test derives
authority from them. Product behavior is specified by the active architecture,
policy-language, conformance, and security documents under `docs/`.
