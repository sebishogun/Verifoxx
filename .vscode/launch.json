{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug NornRune",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}/cmd/nornrune",
      "cwd": "${workspaceFolder}",
      "args": [
        "debug-worker",
        "--socket",
        "${workspaceFolder}/.nornrune/debug.sock"
      ],
      "buildFlags": "-tags=debug -gcflags='all=-N -l'"
    }
  ]
}
